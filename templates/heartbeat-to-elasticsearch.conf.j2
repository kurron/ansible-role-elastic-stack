input {
    heartbeat {
        type => "heartbeat"
        id => "heartbeat"
        message => "sequence"
    }
}

output {
    elasticsearch {
        hosts => ["{{ ansible_enp0s8.ipv4.address }}"]
    }
}
