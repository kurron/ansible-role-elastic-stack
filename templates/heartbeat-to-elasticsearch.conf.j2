input {
    heartbeat {
        type => "heartbeat"
        id => "heartbeat"
        message => "sequence"
    }
}

output {
    elasticsearch {
        hosts => ["{{ elastic_logstash_elasticsearch_host }}"]
    }
}
